import 'package:flutter/material.dart';

class SubTitleIntroSection extends StatelessWidget {
  const SubTitleIntroSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      "Fresh items everyday",
      style: TextStyle(
        color: Colors.grey[600],
      ),
    );
  }
}
